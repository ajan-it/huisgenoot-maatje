// Central app configuration (no environment variables to keep it simple)
// Update these values for your environment. In production, set the webhook URL and secret.
export const POLICY_VERSION = "1.0";

// Step 8 webhook (Make/Integromat) configuration
export const PLAN_WEBHOOK_URL = ""; // e.g., https://hook.eu1.make.com/xxxxxxxxxxxxxxxx
export const PLAN_WEBHOOK_SECRET = ""; // Must match the secret configured in Make

export const TIMEZONE = "Europe/Amsterdam";
